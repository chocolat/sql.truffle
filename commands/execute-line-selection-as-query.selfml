(trigger
    (key [[ctrl][shift]Q]))

(name [Execute Line / Selection as Query])

(input selection line)

(output html)

(save nothing)

(script [query=${TM_SELECTED_TEXT:-$TM_CURRENT_LINE}

ruby "$TM_BUNDLE_SUPPORT/bin/db_browser.rb" --query="$(tr '\n' ' ' <<< "$query")"
])